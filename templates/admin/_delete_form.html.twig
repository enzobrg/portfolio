
<form method="post" action="{{ path('home_projects_delete', {'id': project.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project.id) }}">
    <button class="btn text-white fs-4 mt-4">Delete ? <i class="far fa-trash-alt"></i></button>
</form>
